apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: istio-gateway-bot
  namespace: argocd
  labels:
    scope: system
spec:
    targetRevision: "$(ISTIO_VERSION)"
    helm:
      valueFiles:
      - values.yaml
